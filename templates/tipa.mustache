<!DOCTYPE html>
<html>

<head>
    <title>{{data}}</title>
    <script src="http://code.jquery.com/jquery-3.3.1.min.js" integrity="sha256-FgpCb/KJQlLNfOu91ta32o/NMZxltwRo8QtmkMRdAu8=" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/2.0.4/socket.io.js"></script>
    <style>
    body,
    html {
        margin: 0;
        min-height: 100%;
        height: 100%;
        width: 100%;
    }
    @font-face {
        font-family: fontt;
        src: url(http://18.221.71.184/s/public/7fonts.ru_CaviarDreams.ttf);
    }
    .container {
        height: 100%;
        text-align: center;
    }

    .container img {
        height: calc(100% - 10vh);
    }

    .container p {
        padding: 0 15px;
        font-weight: bold;
        font-family: fontt;
        margin: 0;
        display: inline-block;
        color: white;
        border-radius: 10px;
        background: rgba(55,55,55,.6);
        text-align: center;
        font-size: 8vh;
    }
    </style>
</head>

<body>
    <div class="container">
    </div>
    <script>
    var socket = io.connect('http://18.221.71.184');
    socket.on('tipa{{data}}', function(data) {
        var num = parseInt(data.split('tipped')[1].split('tokens')[0])
        console.log(num)
        if (num < 10) {
            removing(3)
            $('.container').html('<img src="http://18.221.71.184/s/public/giphy1.gif"><p>' + data.replace(":", ' ') + '</p>')
        }
        if (num > 99) {
            removing(9)
            $('.container').html('<img src="http://18.221.71.184/s/public/giphy3.gif"><p>' + data.replace(":", ' ') + '</p>')
            return;
        }
        if (num >= 10) {
            removing(6)
            $('.container').html('<img src="http://18.221.71.184/s/public/giphy2.gif"><p>' + data.replace(":", ' ') + '</p>')
        }
    });
    var timer;
    function removing(e){
        clearTimeout(timer)
        timer = setTimeout(function() {
            $('.container').html('')
        }, e*1000);
    }
    </script>
</body>

</html>